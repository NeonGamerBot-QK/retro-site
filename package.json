{
  "name": "qr-site",
  "module": "src/index.php",
  "type": "module",
  "scripts": {
    "build": "bun run build:files && bun run minify",
    "dev": "bun run scripts/dev.ts",
    "build:files": "bun run scripts/build.ts",
    "minify": "bun run scripts/minify.ts",
    "start": "serve ./build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "php": "^1.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "html-minifier": "^4.0.0",
    "qrcode": "^1.5.4",
    "serve": "^14.2.3",
    "sharp": "^0.33.5"
  }
}
